This fixture is intentionally empty of a CLAUDE.md — the "missing" drift
class covers the case where harness.yaml declares instruction content but the
expected deployed file does not exist at all. See `__tests__/fix.test.ts`.
