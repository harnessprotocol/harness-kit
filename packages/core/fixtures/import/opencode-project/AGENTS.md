This project uses OpenCode as its primary coding agent. Always run `pnpm test`
before committing, and never edit generated files under `dist/` directly.

## Deploy

Deploys are triggered by pushing a tag matching `v*`.
