Some hand-written preamble the user added themselves, above the generated block.

<!-- BEGIN harness:acme:operational -->
This text was generated by a previous harness-kit compile — it must NOT be
re-imported as user content, since it's our own output being read back.
<!-- END harness:acme:operational -->

A trailing note the user appended after the generated block.
