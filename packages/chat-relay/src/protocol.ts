// Re-export protocol types from shared for convenience
export type {
  ClientMessage,
  ServerMessage,
  AnyMessage,
  Member,
  ChatMessage,
  ShareMessage,
  SystemMessage,
  ShareAction,
} from "@harness-kit/shared";
