// Re-export protocol types from shared for convenience
export type {
  AnyMessage,
  ChatMessage,
  ClientMessage,
  Member,
  ServerMessage,
  ShareAction,
  ShareMessage,
  SystemMessage,
} from "@harness-kit/shared";
